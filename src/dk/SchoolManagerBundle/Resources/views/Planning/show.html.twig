{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Planning</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dateplanning</th>
                <td>{{ entity.dateplanning|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('planning') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('planning_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
