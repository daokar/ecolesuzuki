{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tarif list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Typetarif</th>
                <th>Montanttarif</th>
                <th>Datetarif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tarif_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.typetarif }}</td>
                <td>{{ entity.montanttarif }}</td>
                <td>{% if entity.datetarif %}{{ entity.datetarif|date('d/m/Y') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tarif_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tarif_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tarif_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
